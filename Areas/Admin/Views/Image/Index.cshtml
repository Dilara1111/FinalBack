@using Final_Back.Models;
@model List<ImgContainer>
<div class="card">
    <div class="card-body">
        <div class="d-flex; justify-content-lg-between">
            <h5 class="card-title">Images</h5>
            <a asp-action="create" asp-controller="image" asp-area="admin" class="col-2 d-sm-inline-block btn btn-outline-success">Add new image</a>
        </div>
        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Photo</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        foreach (var image in Model)
                        {
                            <tr>
                                <th>@image.Id</th>
                                <td><img style="width:100px;height:100px;" src="~/assets/img/@image.Image"></td>                                
                                <th>
                                    <a asp-controller="image" asp-action="update" asp-area="admin" asp-route-id="@image.Id" class="btn btn-secondary">Update</a>
                                    <form class="d-inline" method="post" asp-controller="image" asp-action="delete" asp-area="admin" asp-route-id="@image.Id">
                                        <button class="btn btn-secondary" type="submit">Delete</button>
                                    </form>
                                </th>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
